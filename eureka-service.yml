server:
    port: ${PORT:8761}

eureka: 
  client: 
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    org.springframework.security: INFO

endpoints:
  shutdown:
    enabled: true